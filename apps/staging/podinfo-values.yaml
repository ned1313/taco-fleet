apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: cluster-config
spec:
  targetNamespace: podinfostage
  releaseName: podinfostage
  chart:
    spec:
      version: ">=1.0.0"